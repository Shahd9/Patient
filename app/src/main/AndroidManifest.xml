<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msaproject.patient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.recipe_details.RecipeDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".ui.chronic_diseases.ChronicDiseasesActivity" />

        <activity android:name=".ui.appointments.appointment_list.AppointmentsListActivity" />

        <activity android:name=".ui.appointments.appointment_details.AppointmentDetailsActivity" />

        <activity android:name=".ui.doctor_details.DoctorsDetailsActivity" />

        <activity android:name=".ui.edit_profile.EditProfileActivity" />

        <activity android:name=".ui.medical_files.MedicalFilesActivity" />

        <activity android:name=".ui.phone_auth.PhoneAuthActivity" />

        <activity
            android:name=".ui.onboarding.OnBoardingActivity"
            android:theme="@style/AppTheme.NoStatusBar" />

        <activity android:name=".ui.host.HostActivity" />

        <activity android:name=".ui.complete_register.CompleteRegisterActivity" />

        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/AppTheme.NoStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>